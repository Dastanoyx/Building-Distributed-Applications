# Copy this file to .env and adjust if you like. .env is git-ignored on purpose:
# it is the only place where credentials live (Session 10).
CATALOG_DB_PASSWORD=dev-catalog-password
ORDER_DB_PASSWORD=dev-order-password
NOTIFICATION_DB_PASSWORD=dev-notification-password
GRAFANA_PASSWORD=admin
