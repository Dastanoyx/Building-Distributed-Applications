<?xml version="1.0" encoding="UTF-8"?>
<!--
  OrderFlow — capstone project for the course "Building Distributed Applications".
  Course by Yassin Ghariani.

  This parent POM holds the versions and the module list. Every service below is a
  separate Spring Boot application with its own database, its own Dockerfile and its
  own lifecycle: the only thing they share is the `common-events` module, which
  contains the event records exchanged over Kafka.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Spring Boot's parent gives us dependency management for the whole Spring ecosystem. -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.4</version>
    <relativePath/>
  </parent>

  <groupId>dev.orderflow</groupId>
  <artifactId>orderflow</artifactId>
  <version>1.0.0</version>
  <packaging>pom</packaging>
  <name>orderflow</name>
  <description>Distributed order platform used throughout the course</description>

  <modules>
    <module>common-events</module>
    <module>catalog-service</module>
    <module>order-service</module>
    <module>payment-service</module>
    <module>notification-service</module>
    <module>gateway</module>
  </modules>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <spring-cloud.version>2023.0.3</spring-cloud.version>
    <testcontainers.version>1.20.1</testcontainers.version>
    <wiremock.version>3.9.1</wiremock.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- Spring Cloud brings the gateway and the Resilience4j circuit breaker starter. -->
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <!-- Every module gets the test starter: JUnit 5, AssertJ, Mockito, MockMvc. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <configuration>
            <!-- Keeps the jar layered so Docker can cache dependencies separately (Session 04). -->
            <layers><enabled>true</enabled></layers>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
